# Core dependencies
numpy==1.24.4  # Lighter version
pandas==2.0.3  # Slightly older but lighter

# ML/DL dependencies
scikit-learn==1.3.2  # Lighter version
joblib==1.3.2  # Required by scikit-learn

# Image processing
opencv-python-headless==4.8.0.76  # Minimal version with headless support
Pillow==10.0.1  # Lighter alternative for basic image processing

# Visualization (only if absolutely needed)
plotly==5.16.1  # Lighter version
matplotlib==3.7.2  # Only if absolutely needed

# Database
psycopg2-binary==2.9.9

# Web framework (if needed)
fastapi==0.103.1  # Lighter alternative to streamlit for API endpoints
uvicorn==0.23.2  # ASGI server for FastAPI

# Add specific PyTorch version if needed
# torch==2.0.1 --index-url https://download.pytorch.org/whl/cpu
